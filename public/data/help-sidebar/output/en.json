[
 {
  "kind": "heading",
  "sectionSlug": "motion",
  "heading": "Motion"
 },
 {
  "slug": "motion_turn-degrees",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.turn_degrees(`angle`)",
  "help": "Turn the `self` Sprite `angle` degrees anticlockwise (counter-clockwise).  To turn clockwise, use a negative number for `angle`.\n\n```\n# Turn a quarter-turn anticlockwise:\nself.turn_degrees(90)\n```\n\nIf you prefer, you can *augmented assign* to `self.direction`:\n\n```\nself.direction += 90\n```",
  "scratch": "turn ccw [90] degrees"
 },
 {
  "slug": "motion_point-degrees",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.point_degrees(`angle`)",
  "help": "Turn the `self` Sprite to point in the direction given by `angle`, which measures degrees anticlockwise (counter-clockwise) from its original orientation.  To give a direction in terms of rotation clockwise, use a negative value for `angle`.\n\n```\n# Point (1/8)th turn clockwise\n# compared to original costume:\nself.point_degrees(-45)\n```\n\nIf you prefer, you can assign to `self.direction`:\n\n```\nself.direction = -45\n```",
  "scratch": "point in direction [-45]"
 },
 {
  "slug": "motion_point-towards-mouse",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.point_towards_mouse()",
  "help": "Turn the `self` Sprite to point in the direction of the mouse pointer.\n\n```\n# Point towards the mouse pointer:\nself.point_towards_mouse()\n```",
  "scratch": "point towards (mouse pointer v)"
 },
 {
  "slug": "motion_go-to-xy",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.go_to_xy(`x`, `y`)",
  "help": "Move the `self` Sprite to the position (`x`, `y`) on the Stage.\n\n```\n# Move to near top left:\nself.go_to_xy(-200, 150)\n```",
  "scratch": "go to x: [-200] y: [150]"
 },
 {
  "slug": "motion_go-to-mouse",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.go_to_mouse()",
  "help": "Move the `self` Sprite to the position of the mouse pointer.\n\n```\n# Move to the mouse pointer:\nself.go_to_mouse()\n```",
  "scratch": "go to (mouse pointer v)"
 },
 {
  "slug": "motion_go-to-random-position",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.go_to_random_position()",
  "help": "Move the `self` Sprite to a random position on the stage.\n\n```\n# Go somewhere random:\nself.go_to_random_position()\n```",
  "scratch": "go to (random position v)"
 },
 {
  "slug": "motion_glide-to-xy",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.glide_to_xy(`x`, `y`, `seconds`)",
  "help": "Glide the `self` Sprite smoothly to the position (`x`, `y`) on the stage.  The movement will take `seconds` seconds.\n\n```\n# Take 2½ seconds to move smoothly\n# to the centre of the stage:\nself.glide_to_xy(0, 0, 2.5)\n```",
  "scratch": "glide [2.5] secs to x: [0] y: [0]"
 },
 {
  "slug": "motion_glide-to-mouse",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.glide_to_mouse(`seconds`)",
  "help": "Glide the `self` Sprite smoothly to the position of the mouse pointer.  The movement will take `seconds` seconds.\n\n```\n# Take 2½ seconds to move smoothly\n# to the mouse pointer:\nself.glide_to_mouse(2.5)\n```",
  "scratch": "glide [2.5] secs to (mouse pointer v)"
 },
 {
  "slug": "motion_change-x",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.change_x(`dx`)",
  "help": "Move the `self` Sprite horizontally by `dx`.  If `dx` is positive, move right; if `dx` is negative, move left.\n\n```\n# Move left a bit:\nself.change_x(-20)\n```\n\nIf you prefer, you can *augmented assign* to `self.x_position`:\n\n```\nself.x_position -= 20\n```",
  "scratch": "change x by [-20]"
 },
 {
  "slug": "motion_set-x",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.set_x(`x`)",
  "help": "Move the `self` Sprite horizontally to the `x` position.\n\n```\n# Move horizonally to near the\n# left edge of the stage:\nself.set_x(-220)\n```\n\nIf you prefer, you can assign to `self.x_position`:\n\n```\nself.x_position = -220\n```",
  "scratch": "set x to [-220]"
 },
 {
  "slug": "motion_change-y",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.change_y(`dy`)",
  "help": "Move the `self` Sprite vertically by `dy`.  If `dy` is positive, move up; if `dy` is negative, move down.\n\n```\n# Move up a bit:\nself.change_y(25)\n```\n\nIf you prefer, you can *augmented assign* to `self.y_position`:\n\n```\nself.y_position += 25\n```",
  "scratch": "change y by [25]"
 },
 {
  "slug": "motion_set-y",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.set_y(`y`)",
  "help": "Move the `self` Sprite vertically to the `y` position.\n\n```\n# Move vertically to near bottom\n# of stage:\nself.set_y(-150)\n```\n\nIf you prefer, you can assign to `self.y_position`:\n\n```\nself.y_position = -150\n```",
  "scratch": "set y to [-150]"
 },
 {
  "slug": "motion_x-position",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.x_position",
  "help": "The horizontal position (`x` coordinate) of the `self` Sprite.\n\n```\n# Say how far left/right I am:\nself.say(self.x_position)\n```\n\nYou can assign to `self.x_position` to set the `x` coordinate of this sprite:\n\n```\n# Move sideways to three-quarters of the\n# way from left to right:\nself.x_position = 120\n```",
  "scratch": "(x position)"
 },
 {
  "slug": "motion_y-position",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.y_position",
  "help": "The vertical position (`y` coordinate) of the `self` Sprite.\n\n```\n# Print how low/high I am to\n# \"Output\" pane:\nprint(self.y_position)\n```\n\nYou can assign to `self.y_position` to set the `y` coordinate of this sprite:\n\n```\n# Move up or down to three-quarters of\n# the way from bottom to top:\nself.y_position = 90\n```",
  "scratch": "(y position)"
 },
 {
  "slug": "motion_direction",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.direction",
  "help": "The direction the `self` Sprite is currently pointing, measured in degrees anticlockwise (counterclockwise) from its original orientation.\n\n```\n# Say what direction I'm pointing:\nself.say(self.direction)\n```\n\nYou can assign to `self.direction` to point this sprite in a particular direction:\n\n```\n# Point the opposite direction to default:\nself.direction = 180\n```",
  "scratch": "(direction)"
 },
 {
  "kind": "heading",
  "sectionSlug": "looks",
  "heading": "Looks"
 },
 {
  "slug": "looks_say-for-seconds",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.say_for_seconds(`text`, `seconds`)",
  "help": "Show a speech bubble for the `self` Sprite, saying `text`, lasting for `seconds` seconds.\n\n```\n# Ask nicely for an apple:\nself.say_for_seconds(\"Apple!\", 2)\nself.say_for_seconds(\"Please?\", 2)\n```",
  "scratch": "say [Apple!] for [2] seconds"
 },
 {
  "slug": "looks_say",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.say(`text`)",
  "help": "Show a speech bubble for the `self` Sprite, saying `text`.\n\n```\n# Greet everyone:\nself.say(\"Hello world!\")\n```\n\nIf `text` is the empty string, remove the speech bubble for the `self` Sprite.  If the Sprite doesn't currently have a speech bubble, do nothing.\n\n```\n# Stop talking:\nself.say(\"\")\n```",
  "scratch": "say [Hello world!]"
 },
 {
  "slug": "looks_switch-costume",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.switch_costume(`name_or_index`)",
  "help": {
   "flat": "Switch to one of the costumes listed in this Sprite's *Costumes* variable.  You can do this either by giving the costume's *name*, or its *number*.\n\n```\n# Switch to smiling costume:\nself.switch_costume(\"smiling\")\n```\n\nIf you want to use a *number* to say which costume, remember that Python starts counting entries in a list at *zero*.\n\n```\n# Switch to first costume:\nself.switch_costume(0)\n```\n\n```\n# Switch to second costume:\nself.switch_costume(1)\n```\n\nIf you prefer, you can assign to `self.costume_number` or `self.costume_name`:\n\n```\nself.costume_number = 3\n# or\nself.costume_name = \"sparkly\"\n```",
   "per-method": "Switch to one of this Sprite's costumes.  You can do this either by giving the costume's *name*, or its *number*.\n\n```\n# Switch to smiling costume:\nself.switch_costume(\"smiling.png\")\n```\n\nIf you want to use a *number* to say which costume, remember that Python starts counting entries in a list at *zero*.\n\n```\n# Switch to first costume:\nself.switch_costume(0)\n```\n\n```\n# Switch to second costume:\nself.switch_costume(1)\n```\n\nIf you prefer, you can assign to `self.costume_number` or `self.costume_name`:\n\n```\nself.costume_number = 3\n# or\nself.costume_name = \"sparkly.png\"\n```"
  },
  "scratch": "switch costume to (smiling v)"
 },
 {
  "slug": "looks_switch-backdrop",
  "kind": "block",
  "actorKind": "stage",
  "python": "self.switch_backdrop(`name_or_index`)",
  "help": {
   "flat": "Switch to one of the backdrops listed in your Stage's *Backdrops* variable.  You can do this either by giving the costume's *name*, or its *number*.\n\n```\n# Switch to sunny backdrop:\nself.switch_backdrop(\"sunny\")\n```\n\nIf you want to use a *number* to say which backdrop, remember that Python starts counting entries in a list at *zero*.\n\n```\n# Switch to first backdrop:\nself.switch_backdrop(0)\n```\n\n```\n# Switch to second backdrop:\nself.switch_backdrop(1)\n```\n\nIf you prefer, you can assign to `self.backdrop_number` or `self.backdrop_name`:\n\n```\nself.backdrop_number = 3\n# or\nself.backdrop_name = \"sparkly\"\n```",
   "per-method": "Switch to one of the Stage's backdrops.  You can do this either by giving the costume's *name*, or its *number*.\n\n```\n# Switch to sunny backdrop:\nself.switch_backdrop(\"sunny.jpg\")\n```\n\nIf you want to use a *number* to say which backdrop, remember that Python starts counting entries in a list at *zero*.\n\n```\n# Switch to first backdrop:\nself.switch_backdrop(0)\n```\n\n```\n# Switch to second backdrop:\nself.switch_backdrop(1)\n```\n\nIf you prefer, you can assign to `self.backdrop_number` or `self.backdrop_name`:\n\n```\nself.backdrop_number = 3\n# or\nself.backdrop_name = \"sparkly.png\"\n```"
  },
  "scratch": "switch backdrop to (sunny v)"
 },
 {
  "slug": "looks_next-costume",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.next_costume(`n_steps=1`)",
  "help": "Without `n_steps`, switch the `self` Sprite to the costume after the current one in the `Costumes` list.  If the Sprite was already wearing the last costume in the list, then switch back to the first one in the list.\n\n```\n# Switch to next costume:\nself.next_costume()\n```\n\nYou can also go forward more than one costume, or go *backwards* in the `Costumes` list, by giving `n_steps`.  With this usage, the `self` Sprite will switch to the costume `n_steps` after the current one.  If this would take the Sprite beyond the end of its `Costumes` list, then loop round to the start of the list, as if it was arranged in a circle.\n\n```\n# Switch to next-but-one costume:\nself.next_costume(2)\n```\n\nUse a negative number for `n_steps` to switch to an *earlier* costume in the list.\n\n```\n# Switch to previous costume:\nself.next_costume(-1)\n```",
  "scratch": "next costume"
 },
 {
  "slug": "looks_next-backdrop",
  "kind": "block",
  "actorKind": "stage",
  "python": "self.next_backdrop(`n_steps=1`)",
  "help": "Without `n_steps`, switch your Stage to the backdrop after the current one in the `Backdrops` list.  If the Stage was already showing the last backdrop in the list, then switch back to the first one in the list.\n\n```\n# Switch to next backdrop:\nself.next_backdrop()\n```\n\nYou can also go forward more than one backdrop, or go *backwards* in the `Backdrops` list, by giving `n_steps`.  With this usage, your Stage will switch to the backdrop `n_steps` after the current one.  If this would take the Stage beyond the end of its `Backdrops` list, then loop round to the start of the list, as if it was arranged in a circle.\n\n```\n# Switch to next-but-one backdrop:\nself.next_backdrop(2)\n```\n\nUse a negative number for `n_steps` to switch to an *earlier* backdrop in the list.\n\n```\n# Switch to previous backdrop:\nself.next_backdrop(-1)\n```",
  "scratch": "next backdrop"
 },
 {
  "slug": "looks_set-size",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.set_size(`size`)",
  "help": "Set the `self` Sprite's size to `size`.  Scratch uses percentages for sizes, but in Pytch, `1` means the Sprite's normal size, `0.5` means half size, `2` means double size, and so on.\n\n```\n# Become three-quarter size:\nself.set_size(0.75)\n```\n\nIf you prefer, you can assign to `self.size`:\n\n```\nself.size = 0.75\n```",
  "scratch": "set size to (75) %"
 },
 {
  "slug": "looks_show",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.show()",
  "help": "Make the `self` Sprite visible.\n\n```\n# Appear:\nself.show()\n```",
  "scratch": "show"
 },
 {
  "slug": "looks_hide",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.hide()",
  "help": "Make the `self` Sprite invisible.\n\n```\n# Disappear:\nself.hide()\n```",
  "scratch": "hide"
 },
 {
  "slug": "looks_go-to-front-layer",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.go_to_front_layer()",
  "help": "Move the `self` Sprite to the front drawing layer, making it appear 'closer' to the player than everything else.\n\n```\n# Move in front of everything:\nself.go_to_front_layer()\n```",
  "scratch": "go to [front v] layer"
 },
 {
  "slug": "looks_go-to-back-layer",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.go_to_back_layer()",
  "help": "Move the `self` Sprite to the back drawing layer, making it appear 'further' from the player than everything else.\n\n```\n# Move behind everything:\nself.go_to_back_layer()\n```",
  "scratch": "go to [back v] layer"
 },
 {
  "slug": "looks_go-forward-layers",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.go_forward_layers(`n_layers`)",
  "help": "Move the `self` Sprite `n_layers` layers forwards in the drawing order, appearing 'closer' to the player.\n\n```\n# Move three layers nearer front:\nself.go_forward_layers(3)\n```",
  "scratch": "go [forward v] (3) layers"
 },
 {
  "slug": "looks_go-backward-layers",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.go_backward_layers(`n_layers`)",
  "help": "Move the `self` Sprite `n_layers` layers backwards in the drawing order, appearing 'further' from the player.\n\n```\n# Move four layers nearer back:\nself.go_backward_layers(4)\n```",
  "scratch": "go [backward v] (4) layers"
 },
 {
  "slug": "looks_costume-number",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.costume_number",
  "help": "The position in its `Costumes` list of the costume which the `self` Sprite is wearing.  Remember that Python counts list entries starting from *zero*. For example, if a Sprite is wearing its *third* costume, `self.costume_number` will be `2`.\n\n```\n# Say my costume number:\nself.say(self.costume_number)\n```\n\nYou can assign to `self.costume_number` to switch to a particular costume by (zero-based) *index*:\n\n```\n# Wear my fifth costume:\nself.costume_number = 4\n```",
  "scratch": "(costume (number v))"
 },
 {
  "slug": "looks_backdrop-number",
  "kind": "block",
  "actorKind": "stage",
  "python": "self.backdrop_number",
  "help": "The position in its `Backdrops` list of the backdrop your Stage is showing.  Remember that Python counts list entries starting from *zero*. For example, if the Stage is showing its *third* backdrop, `self.backdrop_number` will be `2`.\n\n```\n# Print my backdrop number\n# to the \"Output\" pane:\nprint(self.backdrop_number)\n```\n\nYou can assign to `self.backdrop_number` to switch to a particular backdrop by (zero-based) *index*:\n\n```\n# Wear my fifth backdrop:\nself.backdrop_number = 4\n```",
  "scratch": "(backdrop (number v))"
 },
 {
  "slug": "looks_costume-name",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.costume_name",
  "help": {
   "flat": "The name of the costume which the `self` Sprite is wearing.\n\n```\n# Print my costume name to the\n# \"Output\" pane:\nprint(self.costume_name)\n```\n\nYou can assign to `self.costume_name` to switch to a particular costume by name:\n\n```\n# Wear my favourite costume:\nself.costume_name = \"sparkly\"\n```",
   "per-method": "The name of the costume which the `self` Sprite is wearing.\n\n```\n# Print my costume name to the\n# \"Output\" pane:\nprint(self.costume_name)\n```\n\nYou can assign to `self.costume_name` to switch to a particular costume by name:\n\n```\n# Wear my favourite costume:\nself.costume_name = \"sparkly.png\"\n```"
  },
  "scratch": "(costume (name v))"
 },
 {
  "slug": "looks_backdrop-name",
  "kind": "block",
  "actorKind": "stage",
  "python": "self.backdrop_name",
  "help": {
   "flat": "The name of the backdrop which the Stage is showing.\n\n```\n# Print my backdrop name to the\n# \"Output\" pane:\nprint(self.backdrop_name)\n```\n\nYou can assign to `self.backdrop_name` to switch to a particular backdrop by name:\n\n```\n# Use my favourite backdrop:\nself.backdrop_name = \"sparkly\"\n```",
   "per-method": "The name of the backdrop which the Stage is showing.\n\n```\n# Print my backdrop name to the\n# \"Output\" pane:\nprint(self.backdrop_name)\n```\n\nYou can assign to `self.backdrop_name` to switch to a particular backdrop by name:\n\n```\n# Use my favourite backdrop:\nself.backdrop_name = \"sparkly.png\"\n```"
  },
  "scratch": "(backdrop (name v))"
 },
 {
  "slug": "looks_size",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.size",
  "help": "The current size of the `self` Sprite, where `1` is full size, `0.5` is half-size, `2` is double-size, and so on.\n\n```\n# Say how big I am:\nself.say(self.size)\n```\n\nYou can assign to `self.size` to set this sprite's size:\n\n```\n# Become twice normal size:\nself.size = 2.0\n```",
  "scratch": "(size)"
 },
 {
  "kind": "heading",
  "sectionSlug": "sound",
  "heading": "Sound"
 },
 {
  "slug": "sound_play-sound-until-done",
  "kind": "block",
  "python": "self.play_sound_until_done(`sound`)",
  "help": {
   "flat": "Start a sound playing and wait for it to finish.  The given `sound` can either be the name of the sound or its index (zero-based position) in the Sprite's (or Stage's) list of sounds.  This function will not return until the entire sound has played, so your code will not move on to its next statement until the sound is done playing.\n\n```\n# (This example is for a Sprite)\n# Make noise, then disappear:\nself.play_sound_until_done(\"splash\")\nself.hide()\n```",
   "per-method": {
    "sprite": "Start a sound playing and wait for it to finish.  The given `sound` can either be the name of the sound or its index (zero-based position) in the Sprite's list of sounds.  This function will not return until the entire sound has played, so your Sprite will not move on to the next statement in the code until the sound is done playing.\n\n```\n# Make noise, then disappear:\nself.play_sound_until_done(\"splash.mp3\")\nself.hide()\n```",
    "stage": "Start a sound playing and wait for it to finish.  The given `sound` can either be the name of the sound or its index (zero-based position) in the Stage's list of sounds.  This function will not return until the entire sound has played, so the Stage will not move on to the next statement in the code until the sound is done playing.\n\n```\n# Make noise, then change scenery:\nself.play_sound_until_done(\"splash.mp3\")\nself.next_backdrop()\n```"
   }
  },
  "scratch": "play sound (splash v) until done"
 },
 {
  "slug": "sound_start-sound",
  "kind": "block",
  "python": "self.start_sound(`sound`)",
  "help": {
   "flat": "Start a sound playing.  The given `sound` can either be the name of the sound or its index (zero-based position) in the Sprite's (or Stage's) list of sounds.  Once the sound has started, the Sprite (or Stage) will straight away move on to the next statement in your code.\n\n```\n# (This example is for a Sprite.)\n# Start playing tune, and glide\n# to centre while tune is playing:\nself.start_sound(\"my-song\")\nself.glide_to_xy(0, 0, 1.5)\n```",
   "per-method": {
    "sprite": "Start a sound playing.  The given `sound` can either be the name of the sound or its index (zero-based position) in the Sprite's list of sounds.  Once the sound has started, the Sprite will straight away move on to the next statement in your code.\n\n```\n# Start playing tune, and glide\n# to centre while tune is playing:\nself.start_sound(\"my-song.mp3\")\nself.glide_to_xy(0, 0, 1.5)\n```",
    "stage": "Start a sound playing.  The given `sound` can either be the name of the sound or its index (zero-based position) in the Stage's list of sounds.  Once the sound has started, the Stage will straight away move on to the next statement in your code.\n\n```\n# Start playing tune, and count up\n# the score while tune is playing:\nself.start_sound(\"my-song.mp3\")\nfor i in range(5):\n    self.score += 1\n    self.wait_seconds(1)\n```"
   }
  },
  "scratch": "start sound (my-song v)"
 },
 {
  "slug": "sound_set-sound-volume",
  "kind": "block",
  "python": "self.set_sound_volume(`volume`)",
  "help": {
   "flat": "Set the volume for sounds being played by the `self` Sprite (or Stage).  Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Play fairly quietly:\nself.set_sound_volume(0.25)\n```\n\nIf you prefer, you can assign to `self.sound_volume`:\n\n```\nself.sound_volume = 0.25\n```",
   "per-method": {
    "sprite": "Set the volume for sounds being played by this Sprite.  Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Play fairly quietly:\nself.set_sound_volume(0.25)\n```\n\nIf you prefer, you can assign to `self.sound_volume`:\n\n```\nself.sound_volume = 0.25\n```",
    "stage": "Set the volume for sounds being played by the Stage.  Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Play fairly quietly:\nself.set_sound_volume(0.25)\n```\n\nIf you prefer, you can assign to `self.sound_volume`:\n\n```\nself.sound_volume = 0.25\n```"
   }
  },
  "scratch": "set volume to [25] %"
 },
 {
  "slug": "sound_change-sound-volume",
  "kind": "block",
  "python": "self.change_sound_volume(`d_volume`)",
  "help": {
   "flat": "Increase the volume for sounds being played by the `self` Sprite (or Stage) by the given amount (or decrease the volume, if `d_volume` is negative).    Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Play a bit more loudly:\nself.change_sound_volume(0.1)\n```\n\nIf you prefer, you can *augmented assign* to `self.sound_volume`:\n\n```\nself.sound_volume += 0.1\n```",
   "per-method": {
    "sprite": "Increase the volume for sounds being played by this Sprite by the given amount (or decrease the volume, if `d_volume` is negative).    Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Play a bit more loudly:\nself.change_sound_volume(0.1)\n```\n\nIf you prefer, you can *augmented assign* to `self.sound_volume`:\n\n```\nself.sound_volume += 0.1\n```",
    "stage": "Increase the volume for sounds being played by the Stage by the given amount (or decrease the volume, if `d_volume` is negative).    Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Play a bit more loudly:\nself.change_sound_volume(0.1)\n```\n\nIf you prefer, you can *augmented assign* to `self.sound_volume`:\n\n```\nself.sound_volume += 0.1\n```"
   }
  },
  "scratch": "change volume by [10]"
 },
 {
  "slug": "sound_sound-volume",
  "kind": "block",
  "python": "self.sound_volume",
  "help": {
   "flat": "The current volume for sounds played by the `self` Sprite (or Stage).  Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Say how loud I am:\nself.say(self.sound_volume)\n```\n\nYou can assign to `self.sound_volume` to set the volume for this sprite (or the stage):\n\n```\n# Play fairly quietly:\nself.sound_volume = 0.25\n```",
   "per-method": {
    "sprite": "The current volume for sounds played by this Sprite.  Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Say how loud I am:\nself.say(self.sound_volume)\n```\n\nYou can assign to `self.sound_volume` to set this sprite's volume:\n\n```\n# Play fairly quietly:\nself.sound_volume = 0.25\n```",
    "stage": "The current volume for sounds played by the Stage.  Volume values are measured between `0` (silence) and `1` (full volume), which is different from Scratch, which measures between `0%` and `100%`.\n\n```\n# Print message saying how loud\n# my sounds are:\nprint(self.sound_volume)\n```\n\nYou can assign to `self.sound_volume` to set the stage's volume:\n\n```\n# Play fairly quietly:\nself.sound_volume = 0.25\n```"
   }
  },
  "scratch": "(volume)"
 },
 {
  "slug": "sound_stop-all-sounds",
  "kind": "block",
  "python": "self.stop_all_sounds()",
  "help": "Stop all sounds that are currently playing.\n\n```\n# Make everybody be quiet:\nself.stop_all_sounds()\n```\n\n(You might see `pytch.stop_all_sounds()`, which also works, in older code.)",
  "scratch": "stop all sounds"
 },
 {
  "kind": "heading",
  "sectionSlug": "events",
  "heading": "Events"
 },
 {
  "slug": "events_when-green-flag-clicked",
  "kind": "block",
  "python": "@pytch.when_green_flag_clicked",
  "eventDescriptor": {
   "kind": "green-flag"
  },
  "help": {
   "flat": "Ask Pytch to run the method below whenever the green flag is clicked by the player.\n\n```\n# Move to correct position\n# when game starts:\n@pytch.when_green_flag_clicked\ndef go_to_start_position(self):\n    self.go_to_xy(20, 20)\n```",
   "per-method": "Ask Pytch to run code whenever the green flag is clicked by the player."
  },
  "scratch": "when green flag clicked"
 },
 {
  "slug": "events_when-key-pressed",
  "kind": "block",
  "python": "@pytch.when_key_pressed(`key_name`)",
  "eventDescriptor": {
   "kind": "key-pressed",
   "keyName": "b"
  },
  "help": {
   "flat": "Ask Pytch to run the method below whenever the player presses the `key_name` key.\n\n```\n# Bark when player presses \"b\" key:\n@pytch.when_key_pressed(\"b\")\ndef bark(self):\n    self.start_sound(\"woof\")\n```",
   "per-method": "Ask Pytch to run code whenever the player presses a particular key."
  },
  "scratch": "when [b v] key pressed"
 },
 {
  "slug": "events_when-this-sprite-clicked",
  "kind": "block",
  "actorKind": "sprite",
  "python": "@pytch.when_this_sprite_clicked",
  "eventDescriptor": {
   "kind": "clicked"
  },
  "help": {
   "flat": "Ask Pytch to run the method below whenever the player clicks on this Sprite.\n\n```\n# Disappear if player clicks on me:\n@pytch.when_this_sprite_clicked\ndef disappear(self):\n    self.hide()\n```",
   "per-method": "Ask Pytch to run code whenever the player clicks on this Sprite."
  },
  "scratch": "when this sprite clicked"
 },
 {
  "slug": "events_when-stage-clicked",
  "kind": "block",
  "actorKind": "stage",
  "python": "@pytch.when_stage_clicked",
  "eventDescriptor": {
   "kind": "clicked"
  },
  "help": {
   "flat": "Ask Pytch to run the method below whenever the player clicks on the Stage.\n\n```\n# Make noise if player clicks on\n# the Stage:\n@pytch.when_stage_clicked\ndef beep(self):\n    self.start_sound(\"beep\")\n```",
   "per-method": "Ask Pytch to run code whenever the player clicks on the Stage."
  },
  "scratch": "when stage clicked"
 },
 {
  "slug": "events_when-i-receive",
  "kind": "block",
  "python": "@pytch.when_I_receive(`message`)",
  "eventDescriptor": {
   "kind": "message-received",
   "message": "message-1"
  },
  "help": {
   "flat": "Ask Pytch to run the method below whenever any Sprite (or the Stage) broadcasts the `message`.\n\n```\n# Give player a point when they\n# pop a bubble:\n@pytch.when_I_receive(\"pop\")\ndef give_player_point(self):\n    self.score += 1\n    self.say(self.score)\n```",
   "per-method": "Ask Pytch to run code whenever any Sprite (or the Stage) broadcasts a particular message."
  },
  "scratch": "when I receive [pop v]"
 },
 {
  "slug": "events_broadcast",
  "kind": "block",
  "python": "self.broadcast(`message`)",
  "help": {
   "flat": "Broadcast the `message`.  This makes any methods with matching `@pytch.when_I_receive()` run.  Your method immediately continues running after the message has been sent.\n\n```\n# Tell cat it has been caught, and\n# straight away start barking:\nself.broadcast(\"caught-cat\")\nself.start_sound(\"woof\")\n```\n\n(You might see `pytch.broadcast()`, which also works, in older code.)",
   "per-method": {
    "sprite": "Broadcast the `message`.  This runs any code under a matching *when I receive* hat block.  Your code immediately continues running after the message has been sent.\n\n```\n# Tell cat it has been caught, and straight\n# away start barking:\nself.broadcast(\"caught-cat\")\nself.start_sound(\"woof\")\n```\n\n(You might see `pytch.broadcast()`, which also works, in older code.)",
    "stage": "Broadcast the `message`.  This runs any code under a matching *when I receive* hat block.  Your code immediately continues running after the message has been sent.\n\n```\n# Tell cat it has been caught, and straight\n# away show the sky backdrop:\nself.broadcast(\"caught-cat\")\nself.switch_backdrop(\"sky.jpg\")\n```\n\n(You might see `pytch.broadcast()`, which also works, in older code.)"
   }
  },
  "scratch": "broadcast [caught-cat v]"
 },
 {
  "slug": "events_broadcast-and-wait",
  "kind": "block",
  "python": "self.broadcast_and_wait(`message`)",
  "help": {
   "flat": "Broadcast the `message`.  This makes any methods with matching `@pytch.when_I_receive()` run.  Your method then waits until all methods which have responded to that message have finished running.\n\n```\n# Tell the dog to dance, and then\n# praise it when it has finished:\nself.broadcast_and_wait(\"dance\")\nself.say(\"Very good!\")\n```\n\n(You might see `pytch.broadcast_and_wait()`, which also works, in older code.)",
   "per-method": {
    "sprite": "Broadcast the `message`.  This runs any code under a matching *when I receive* hat block.  Your code then waits until all code which has responded to that message has finished running.\n\n```\n# Tell the dog to dance, and then praise it\n# when it has finished:\nself.broadcast_and_wait(\"dance\")\nself.say(\"Very good!\")\n```\n\n(You might see `pytch.broadcast_and_wait()`, which also works, in older code.)",
    "stage": "Broadcast the `message`.  This runs any code under a matching *when I receive* hat block.  Your code then waits until all code which has responded to that message has finished running.\n\n```\n# Tell the dog to dance, and then show the\n# sky backdrop when it has finished:\nself.broadcast_and_wait(\"dance\")\nself.switch_backdrop(\"sky.jpg\")\n```\n\n(You might see `pytch.broadcast_and_wait()`, which also works, in older code.)"
   }
  },
  "scratch": "broadcast [dance v] and wait"
 },
 {
  "kind": "heading",
  "sectionSlug": "control",
  "heading": "Control"
 },
 {
  "slug": "events_wait-seconds",
  "kind": "block",
  "python": "self.wait_seconds(`seconds`)",
  "help": "Pause your code for `seconds` seconds.\n\n```\n# Wait for half a second:\nself.wait_seconds(0.5)\n```\n\n(You might see `pytch.wait_seconds()`, which also works, in older code.)",
  "scratch": "wait [0.5] seconds"
 },
 {
  "slug": "events_repeat",
  "kind": "non-method-block",
  "heading": "Repeat",
  "python": "for i in range(_MV_n_times_MV_):\n    _MV_your_code_MV_",
  "help": "Run the indented code block the given number of times.  In Python, you can use the *loop variable* inside the block — it counts from zero up to one less than the number of times the loop runs, inclusive.\n\n```\n# Count from zero up to nine, writing the\n# numbers to the \"Output\" pane:\nfor i in range(10):\n    print(i)\n```",
  "scratch": "repeat (10)\nend"
 },
 {
  "slug": "events_forever",
  "kind": "non-method-block",
  "heading": "Forever",
  "python": "while True:\n    _MV_your_code_MV_",
  "help": {
   "flat": "Run the indented block of code round and round forever.\n\n```\n# (This example is for a Sprite.)\n# Make sure I am always saying the\n# right score:\nwhile True:\n    self.say(score)\n```",
   "per-method": {
    "sprite": "Run the indented block of code round and round forever.\n\n```\n# Make sure I am always saying the\n# right score:\nwhile True:\n    self.say(score)\n```",
    "stage": "Run the indented block of code round and round forever.\n\n```\n# Keep cycling through my backdrops,\n# changing twice per second:\nwhile True:\n    self.next_backdrop()\n    self.wait_seconds(0.5)\n```"
   }
  },
  "scratch": "forever\nend"
 },
 {
  "slug": "events_if-then",
  "kind": "non-method-block",
  "heading": "If/then",
  "python": "if _MV_your_test_MV_:\n    _MV_code_to_run_if_test_true_MV_",
  "help": {
   "flat": "Check whether `your_test` is true.  If (and only if) it is true, the indented code block runs.\n\n```\n# (This example is for a Sprite.)\n# Show warning if one apple left:\nif n_apples == 1:\n    self.say(\"Last apple!\")\n\n# No matter how many apples left,\n# glide back to centre:\nself.glide_to_xy(0, 0, 1.5)\n```",
   "per-method": {
    "sprite": "Check whether `your_test` is true.  If (and only if) it is true, the indented code block runs.\n\n```\n# Show warning if one apple left:\nif n_apples == 1:\n    self.say(\"Last apple!\")\n\n# No matter how many apples left,\n# glide back to centre:\nself.glide_to_xy(0, 0, 1.5)\n```",
    "stage": "Check whether `your_test` is true.  If (and only if) it is true, the indented code block runs.\n\n```\n# Show warning if one apple left:\nif n_apples == 1:\n    self.switch_backdrop(\"last-apple-warning\")\n\n# No matter how many apples left,\n# print a message to the \"Output\" pane:\nprint(\"Get the next apple!\")\n```"
   }
  },
  "scratch": "if <[n-apples] = (1)> then"
 },
 {
  "slug": "events_if-then-else",
  "kind": "non-method-block",
  "heading": "If/then/else",
  "python": "if _MV_your_test_MV_:\n    _MV_code_to_run_if_test_true_MV_\nelse:\n    _MV_code_to_run_if_test_false_MV_",
  "help": {
   "flat": "Check whether `your_test` is true.  If (and only if) it is, the first indented code block runs.  Otherwise, the second indented code block runs\n\n```\n# (This example is for a Sprite.)\n# Choose costume according to\n# whether player is winning:\nif score > bot_score:\n    self.switch_costume(\"happy\")\nelse:\n    self.switch_costume(\"sad\")\n```\n\nIn Python, you can have more than one test, by using `elif`, which is short for *else if*.  Each test is checked in turn, looking for the first one which is true — then its indented code runs.  If no test is true, the `else` indented code runs.\n\n```\n# Say how well the player did:\nif score >= 20:\n    self.say(\"Excellent!\")\nelif score >= 10:\n    self.say(\"Good!\")\nelse:\n    self.say(\"Keep trying!\")\n```",
   "per-method": {
    "sprite": "Check whether `your_test` is true.  If (and only if) it is, the first indented code block runs.  Otherwise, the second indented code block runs\n\n```\n# Choose costume according to\n# whether player is winning:\nif score > bot_score:\n    self.switch_costume(\"happy\")\nelse:\n    self.switch_costume(\"sad\")\n```\n\nIn Python, you can have more than one test, by using `elif`, which is short for *else if*.  Each test is checked in turn, looking for the first one which is true — then its indented code runs.  If no test is true, the `else` indented code runs.\n\n```\n# Say how well the player did:\nif score >= 20:\n    self.say(\"Excellent!\")\nelif score >= 10:\n    self.say(\"Good!\")\nelse:\n    self.say(\"Keep trying!\")\n```",
    "stage": "Check whether `your_test` is true.  If (and only if) it is, the first indented code block runs.  Otherwise, the second indented code block runs\n\n```\n# Choose backdrop according to\n# whether player is winning:\nif score > bot_score:\n    self.switch_backdrop(\"happy\")\nelse:\n    self.switch_backdrop(\"sad\")\n```\n\nIn Python, you can have more than one test, by using `elif`, which is short for *else if*.  Each test is checked in turn, looking for the first one which is true — then its indented code runs.  If no test is true, the `else` indented code runs.\n\n```\n# Print (to the \"Output\" pane) a message\n# for how well the player did:\nif score >= 20:\n    print(\"Excellent!\")\nelif score >= 10:\n    print(\"Good!\")\nelse:\n    print(\"Keep trying!\")\n```"
   }
  },
  "scratch": "if <(score) > (bot-score)> then\nelse"
 },
 {
  "slug": "events_repeat-until",
  "kind": "non-method-block",
  "heading": "Repeat until",
  "python": "while not _MV_your_test_MV_:\n    _MV_your_code_MV_",
  "help": {
   "flat": "To get the behaviour of Scratch's *repeat until* block, Python has the `while` statement.  This works the other way round to Scratch's *repeat until* block — Python runs the indented code while the test is *true*, compared to Scratch which runs the contained blocks while the test is *false* (i.e., until it's *true*).  Python's `not` operator can be useful, or you can sometimes rephrase the test instead.\n\n```\n# (This example is for a Sprite.)\n# Move slowly up until I touch\n# the Banana:\nwhile not self.touching(Banana):\n    self.change_y(2)\n```\n\nYou can avoid `not` when using comparison operators — instead of `not n_clicks > 3`, you can say:\n\n```\n# Drift left until I have been\n# clicked more than three times:\nwhile n_clicks <= 3:\n    self.change_x(-2)\n```",
   "per-method": {
    "sprite": "To get the behaviour of Scratch's *repeat until* block, Python has the `while` statement.  This works the other way round to Scratch's *repeat until* block — Python runs the indented code while the test is *true*, compared to Scratch which runs the contained blocks while the test is *false* (i.e., until it's *true*).  Python's `not` operator can be useful, or you can sometimes rephrase the test instead.\n\n```\n# Move slowly up until I touch\n# the Banana:\nwhile not self.touching(Banana):\n    self.change_y(2)\n```\n\nYou can avoid `not` when using comparison operators — instead of `not n_clicks > 3`, you can say:\n\n```\n# Drift left until I have been\n# clicked more than three times:\nwhile n_clicks <= 3:\n    self.change_x(-2)\n```",
    "stage": "To get the behaviour of Scratch's *repeat until* block, Python has the `while` statement.  This works the other way round to Scratch's *repeat until* block — Python runs the indented code while the test is *true*, compared to Scratch which runs the contained blocks while the test is *false* (i.e., until it's *true*).  Python's `not` operator can be useful, or you can sometimes rephrase the test instead.\n\n```\n# Keep playing as long as the player\n# has some apples left:\nwhile n_apples > 0:\n    self.broadcast_and_wait(\"play\")\n```\n\nYou can avoid `not` when using comparison operators — instead of `not n_bananas_lost > 5`, you can say:\n\n```\n# Keep playing as long as the player has\n# not lost more than five bananas:\nwhile n_bananas_lost <= 5:\n    self.broadcast_and_wait(\"play\")\n```"
   }
  },
  "scratch": "repeat until <touching (Banana v)?>\nend"
 },
 {
  "slug": "events_wait-until",
  "kind": "non-method-block",
  "heading": "Wait until",
  "python": "while not _MV_your_test_MV_:\n    pass",
  "help": "To get the behaviour of Scratch's *wait until* block, you treat it like an empty *repeat until*, using Python's `while` statement.  This works the other way round to Scratch's *repeat until* block — Python runs the indented code while the test is *true*, compared to Scratch which runs the contained blocks while the test is *false* (i.e., until it's *true*).  Python's `not` operator can be useful, or you can sometimes rephrase the test instead.\n\nTo get *wait until* behaviour, you can use Python's `pass` statement inside the `while`.  The `pass` does nothing, but it is needed because Python needs *some* indented code for the `while` statement.\n\n```\n# Wait until player presses \"d\":\nwhile not self.key_pressed(\"d\"):\n    pass\n```\n\nTo replace *wait until score > 3*, you can avoid having to use `not` by saying:\n```\n# Wait until score > 3:\nwhile score <= 3:\n    pass\n```",
  "scratch": "wait until <key (d v) pressed?>"
 },
 {
  "slug": "events_stop",
  "kind": "block",
  "python": "return",
  "help": {
   "flat": "Stop running your method.  (Pytch does not have a way to say *stop other scripts in sprite*.)\n\n```\n# (This example is for a Sprite.)\n# If already fully grown, do not\n# do anything else:\nif self.costume_name == \"flower\":\n    return\n\n# Continue growing:\nself.next_costume()\n```",
   "per-method": {
    "sprite": "Stop running your script.  (Pytch does not have a way to say *stop other scripts in sprite*.)\n\n```\n# If already fully grown, do not\n# do anything else:\nif self.costume_name == \"flower\":\n    return\n\n# Continue growing:\nself.next_costume()\n```",
    "stage": "Stop running your script.  (Pytch does not have a way to say *stop other scripts in sprite*.)\n\n```\n# If already showing \"game over\", do not\n# do anything else:\nif self.backdrop_name == \"game-over\":\n    return\n\n# Play sorrowful tune:\nself.play_sound_until_done(\"you-lose\")\n```"
   }
  },
  "scratch": "stop [this script v]"
 },
 {
  "slug": "events_stop-all",
  "kind": "block",
  "python": "self.stop_all()",
  "help": "Stop your whole project, exactly as if you had used the red stop button.  (Pytch does not have a way to say *stop other scripts in sprite*.)\n\n```\n# If the player has got enough\n# points, end the whole game:\nif score == 100:\n    self.stop_all()\n```\n\n(You might see `pytch.stop_all()`, which also works, in older code.)",
  "scratch": "stop [all v]"
 },
 {
  "slug": "events_when-i-start-as-a-clone",
  "kind": "block",
  "actorKind": "sprite",
  "python": "@pytch.when_I_start_as_a_clone",
  "eventDescriptor": {
   "kind": "start-as-clone"
  },
  "help": {
   "flat": "Ask Pytch to run the following method whenever a new clone of the Sprite is created.  In that method, the `self` Sprite is the new clone.\n\n```\n# Make a noise as I appear:\n@pytch.when_I_start_as_a_clone\ndef make_noise(self):\n    self.start_sound(\"ping\")\n```",
   "per-method": "Ask Pytch to run code whenever a new clone of the Sprite is created.  In the code, the `self` Sprite is the new clone."
  },
  "scratch": "when I start as a clone"
 },
 {
  "slug": "events_create-clone",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.create_clone()",
  "help": {
   "flat": "Create a new clone of the `self` Sprite.  Any `@pytch.when_I_start_as_a_clone` methods will run for that new clone.\n\n```\n# Make two extra copies of myself\n# across stage:\nself.create_clone()\nself.change_x(50)\nself.create_clone()\nself.change_x(50)\n```",
   "per-method": "Create a new clone of the `self` Sprite.  Any *when I start as a clone* scripts will run for that new clone.\n\n```\n# Make two extra copies of myself\n# across stage:\nself.create_clone()\nself.change_x(50)\nself.create_clone()\nself.change_x(50)\n```"
  },
  "scratch": "create clone of [myself v]"
 },
 {
  "slug": "events_create-clone-of",
  "kind": "block",
  "python": "self.create_clone_of(`sprite_class`)",
  "help": {
   "flat": "Create a new clone of the `sprite_class` Sprite.  Any `@pytch.when_I_start_as_a_clone` methods will run for that new clone.\n\n```\nclass Apple(pytch.Sprite):\n    @pytch.when_key_pressed(\"n\")\n    def make_new_Banana(self):\n        self.create_clone_of(Banana)\n```\n\n(You might see `pytch.create_clone_of()`, which also works, in older code.)",
   "per-method": {
    "sprite": "Create a new clone of the `sprite_class` Sprite.  Any *when I start as a clone* scripts in the `sprite_class` Sprite will run for that new clone.\n\nThis example script is in an `Apple` sprite:\n\n```\n# Make a new Banana:\nself.create_clone_of(Banana)\n```\n\n(You might see `pytch.create_clone_of()`, which also works, in older code.)",
    "stage": "Create a new clone of the `sprite_class` Sprite.  Any *when I start as a clone* scripts in the `sprite_class` Sprite will run for that new clone.\n\n```\n# Make a new Banana:\nself.create_clone_of(Banana)\n```\n\n(You might see `pytch.create_clone_of()`, which also works, in older code.)"
   }
  },
  "scratch": "create clone of [Banana v]"
 },
 {
  "slug": "events_delete-this-clone",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.delete_this_clone()",
  "help": "Delete the `self` clone.  If `self` is the *original* of your Sprite (and so is *not* a clone), then nothing happens — you cannot delete the original of a Sprite.\n\n```\n# This clone is not needed any more:\nself.delete_this_clone()\n```",
  "scratch": "delete this clone"
 },
 {
  "kind": "heading",
  "sectionSlug": "sensing",
  "heading": "Sensing"
 },
 {
  "slug": "sensing_touching",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.touching(`sprite_class`)",
  "help": "Report `True` or `False` according to whether the `self` Sprite is touching either the original `sprite_class` Sprite, or any `sprite_class` clones (if it has any).\n\n```\n# Hide if touching any Apple:\nif self.touching(Apple):\n    self.hide()\n```",
  "scratch": "<touching [Apple v]>"
 },
 {
  "slug": "sensing_touching-mouse",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.touching_mouse",
  "help": "Report `True` or `False` according to whether the `self` Sprite is touching the mouse pointer.\n\n```\n# Hide if touching the mouse pointer:\nif self.touching_mouse:\n    self.hide()\n```",
  "scratch": "<touching (mouse pointer v)>"
 },
 {
  "slug": "sensing_distance-to-mouse",
  "kind": "block",
  "actorKind": "sprite",
  "python": "self.distance_to_mouse",
  "help": "Report the distance between the `self` Sprite and the mouse pointer.\n\n```\n# Shrink if too close to mouse:\nif self.distance_to_mouse < 80:\n    self.set_size(0.5)\n```\n\nIf you prefer, you can assign to `self.size`:\n\n```\nself.size = 0.5\n```",
  "scratch": "(distance to (mouse pointer v))"
 },
 {
  "slug": "sensing_ask-and-wait",
  "kind": "block",
  "python": "self.ask_and_wait(`question`)",
  "help": {
   "flat": "Ask the `question`, wait for the player to type in their answer, and report that answer.  Usually you will assign the player's answer to a variable.\n\nFor a Sprite, if that Sprite is shown, then the question appears in a speech bubble.  If the `self` Sprite is hidden (or if the Stage is doing the asking), the question appears in a box at the bottom of the stage.\n\n```\nname = self.ask_and_wait(\"Name?\")\n```",
   "per-method": {
    "sprite": "Ask the `question`, wait for the player to type in their answer, and report that answer.  Usually you will assign the player's answer to a variable.\n\nIf the Sprite is shown, then the question appears in a speech bubble.  If the Sprite is hidden, the question appears in a box at the bottom of the stage.\n\n```\nname = self.ask_and_wait(\"Name?\")\n```",
    "stage": "Ask the `question`, wait for the player to type in their answer, and report that answer.  Usually you will assign the player's answer to a variable.\n\nThe question appears in a box at the bottom of the stage.\n\n```\nname = self.ask_and_wait(\"Name?\")\n```"
   }
  },
  "scratch": "ask [Name?] and wait"
 },
 {
  "slug": "sensing_key-pressed",
  "kind": "block",
  "python": "self.key_pressed(`key_name`)",
  "help": {
   "flat": "Report `True` or `False` according to whether the player is currently pressing the `key_name` key.\n\n```\n# (This example is for a Sprite.)\n# Choose costume based on whether\n# the \"a\" key is pressed:\nif self.key_pressed(\"a\"):\n    self.switch_costume(\"flying\")\nelse:\n    self.switch_costume(\"walking\")\n```\n\n(You might see `pytch.key_pressed()`, which also works, in older code.)",
   "per-method": {
    "sprite": "Report `True` or `False` according to whether the player is currently pressing the `key_name` key.\n\n```\n# Choose costume based on whether\n# the \"a\" key is pressed:\nif self.key_pressed(\"a\"):\n    self.switch_costume(\"flying.jpg\")\nelse:\n    self.switch_costume(\"walking.jpg\")\n```\n\n(You might see `pytch.key_pressed()`, which also works, in older code.)",
    "stage": "Report `True` or `False` according to whether the player is currently pressing the `key_name` key.\n\n```\n# Choose backdrop based on whether\n# the \"a\" key is pressed:\nif self.key_pressed(\"a\"):\n    self.switch_backdrop(\"cloudy-sky.jpg\")\nelse:\n    self.switch_backdrop(\"underwater.jpg\")\n```\n\n(You might see `pytch.key_pressed()`, which also works, in older code.)"
   }
  },
  "scratch": "<key (a v) pressed?>"
 },
 {
  "slug": "sensing_mouse-down",
  "kind": "block",
  "python": "self.mouse_down",
  "help": {
   "flat": "Report `True` or `False` according to whether the mouse button is currently pressed down.\n\n```\n# (This example is for a Sprite.)\n# Disappear if the mouse button is down:\nif self.mouse_down:\n    self.hide()",
   "per-method": {
    "sprite": "Report `True` or `False` according to whether the mouse button is currently pressed down.\n\n```\n# Disappear if the mouse button is down:\nif self.mouse_down:\n    self.hide()",
    "stage": "Report `True` or `False` according to whether the mouse button is currently pressed down.\n\n```\n# Show starry background if the mouse\n# button is down:\nif self.mouse_down:\n    self.switch_backdrop(\"stars.jpg\")"
   }
  },
  "scratch": "<mouse down?>"
 },
 {
  "slug": "sensing_mouse-x",
  "kind": "block",
  "python": "self.mouse_x",
  "help": "Report the *x* coordinate of the mouse, but restricted to between `-240` and `240` if the mouse is outside that range.\n\n```\n# If the mouse is in the left half\n# of the stage, say so:\nif self.mouse_x < 0:\n    print(\"Left!\")",
  "scratch": "(mouse x)"
 },
 {
  "slug": "sensing_mouse-y",
  "kind": "block",
  "python": "self.mouse_y",
  "help": "Report the *y* coordinate of the mouse, but restricted to between `-180` and `180` if the mouse is outside that range.\n\n```\n# If the mouse is in the top half\n# of the stage, say so:\nif self.mouse_y > 0:\n    print(\"Top!\")",
  "scratch": "(mouse y)"
 },
 {
  "kind": "heading",
  "sectionSlug": "operators",
  "heading": "Operators"
 },
 {
  "slug": "operators_events-add",
  "kind": "block",
  "python": "`value_1` + `value_2`",
  "help": "Give the result of adding the two values together.\n\n```\n# Find total number of pieces of\n# fruit found by the player:\nn_fruits = n_apples + n_oranges\n```",
  "scratch": "((n-apples) + (n-oranges))"
 },
 {
  "slug": "operators_subtract",
  "kind": "block",
  "python": "`value_1` - `value_2`",
  "help": "Give the result of subtracting the second value from the first.\n\n```\n# The player lost a health point:\nnew_health = health - 1\n```",
  "scratch": "((health) - [1])"
 },
 {
  "slug": "operators_multiply",
  "kind": "block",
  "python": "`value_1` * `value_2`",
  "help": "Give the result of multiplying the two values together.\n\n```\n# Each 'jump' is of size 12:\ndistance = n_jumps * 12\n```",
  "scratch": "((n_jumps) * [12])"
 },
 {
  "slug": "operators_divide",
  "kind": "block",
  "python": "`value_1` / `value_2`",
  "help": "Give the result of dividing the first value by the second.  The result can include a decimal part if the first number does not divide exactly by the second.\n\n```\n# Play music louder the higher the\n# player's score is, with a score of\n# 20 being full volume:\nself.set_sound_volume(score / 20)\n```\n\nIf you are working with whole numbers and want to ignore any remainder, you can use Python's `//` operator:\n\n```\n# Award one star for every 50 points:\nn_stars = score // 50\n```",
  "scratch": "((score) / [20])"
 },
 {
  "slug": "operators_randint",
  "kind": "block",
  "python": "random.randint(`lowest`, `highest`)",
  "help": {
   "flat": "To use this function, you must have the line `import random` at the top of your program, just after the `import pytch` line.\n\nPick a random whole number from `lowest` to `highest`, inclusive.\n\n```\n# Pretend to roll a 6-sided die:\nroll = random.randint(1, 6)\n```",
   "per-method": "Pick a random whole number from `lowest` to `highest`, inclusive.\n\n```\n# Pretend to roll a 6-sided die:\nroll = random.randint(1, 6)\n```"
  },
  "scratch": "(pick random (1) to (6))"
 },
 {
  "slug": "operators_uniform",
  "kind": "block",
  "python": "random.uniform(`lowest`, `highest`)",
  "help": {
   "flat": "To use this function, you must have the line `import random` at the top of your program, just after the `import pytch` line.\n\nPick a random number (which might not be a whole number, i.e., it can have a fractional part) from `lowest` to `highest`.\n\n```\n# Pick a random warm temperature:\ntemperature = random.uniform(20.5, 31.75)\n```",
   "per-method": "Pick a random number (which might not be a whole number, i.e., it can have a fractional part) from `lowest` to `highest`.\n\n```\n# Pick a random warm temperature:\ntemperature = random.uniform(20.5, 31.75)\n```"
  },
  "scratch": "(pick random (20.5) to (31.75))"
 },
 {
  "slug": "operators_choice",
  "kind": "block",
  "python": "random.choice(`options`)",
  "scratchIsLong": true,
  "help": {
   "flat": "To use this function, you must have the line `import random` at the top of your program, just after the `import pytch` line.\n\nGive a random element from the list `options`.  For `options`, you can give a literal list, or use a variable which refers to a list.\n\nLiteral list, using []:\n\n```\n# Pick a random cat:\ncat = random.choice([\"Lulu\", \"Daisy\", \"Olive\"])\n```\nUsing a variable:\n\n```\n# Create list of cats:\nall_cats = [\"Lulu\", \"Daisy\", \"Olive\"]\n# Pick one at random:\ncat = random.choice(all_cats)\n```",
   "per-method": "Give a random element from the list `options`.  For `options`, you can give a literal list, or use a variable which refers to a list.\n\nLiteral list, using []:\n\n```\n# Pick a random cat:\ncat = random.choice([\"Lulu\", \"Daisy\", \"Olive\"])\n```\nUsing a variable:\n\n```\n# Create list of cats:\nall_cats = [\"Lulu\", \"Daisy\", \"Olive\"]\n# Pick one at random:\ncat = random.choice(all_cats)\n```"
  },
  "scratch": "(item (pick random (1) to (length of [cats v])) of [cats v])"
 },
 {
  "slug": "operators_larger-than",
  "kind": "block",
  "python": "`value_1` > `value_2`",
  "help": "Report whether the first value is larger than the second value.  You often use this as part of an `if` or `while` statement.\n\n```\n# If player got a good score,\n# congratulate them:\nif score > 50:\n    print(\"Well done!\")\n```\n\nPython also has the `>=` operator, which means 'larger than or equal to':\n```\n# If player is at least 1.5m tall,\n# tell them they can play:\nif height >= 1.5:\n    print(\"Let's play!\")\n```",
  "scratch": "<(score) > [50]>"
 },
 {
  "slug": "operators_smaller-than",
  "kind": "block",
  "python": "`value_1` < `value_2`",
  "help": "Report whether the first value is smaller than the second value.  You often use this as part of an `if` or `while` statement.\n\n```\n# If not many apples left, give a message:\nif n_apples < 5:\n    print(\"Not many apples left!\")\n```\n\nPython also has the `<=` operator, which means 'smaller than or equal to':\n```\n# If player did not beat the\n# high-score, encourage them:\nif score <= high_score:\n    print(\"Try harder!\")\n```",
  "scratch": "<(n-apples) < (5)>"
 },
 {
  "slug": "operators_equals",
  "kind": "block",
  "python": "`value_1` == `value_2`",
  "help": "Report whether the first value is equal to the second value.  You must use *two* equals signs for this in Python.  You often use this as part of an `if` or `while` statement.\n\n```\n# If player and bot got the same\n# score, announce a draw:\nif score == bot_score:\n    print(\"It's a draw!\")\n```",
  "scratch": "<(score) = (bot-score)>"
 },
 {
  "slug": "operators_and",
  "kind": "block",
  "python": "`your_test_1` and `your_test_2`",
  "scratchIsLong": true,
  "help": "Report whether *both* tests are true.  You often use this as part of an `if` or `while` statement.\n\n```\n# If more than three cats AND more\n# than five dogs, say message:\nif n_cats > 3 and n_dogs > 5:\n    print(\"Lots of animals!\")\n```",
  "scratch": "<<(n-cats) > [3]> and <(n-dogs) > [5]>>"
 },
 {
  "slug": "operators_or",
  "kind": "block",
  "python": "`your_test_1` or `your_test_2`",
  "scratchIsLong": true,
  "help": "Report whether the first test *or* the second test (*or* both) is true.  You often use this as part of an `if` or `while` statement.\n\n```\n# If player too low or too fast,\n# warn them:\nif height < 10 or speed > 20:\n    print(\"Watch out!\")\n```",
  "scratch": "<<(height) < [10]> or <(speed) > [20]>>"
 },
 {
  "slug": "operators_not-touching",
  "kind": "block",
  "python": "not `your_test`",
  "help": {
   "flat": "Report whether `your_test` is *false*.  You often use this as part of an `if` or `while` statement.\n\n```\n# (This example is for a Sprite.)\n# If I am not touching the dog,\n# give player instruction:\nif not self.touching(Dog):\n    self.say(\"Go to the dog!\")\n```",
   "per-method": {
    "sprite": "Report whether `your_test` is *false*.  You often use this as part of an `if` or `while` statement.\n\n```\n# If I am not touching the dog,\n# give player instruction:\nif not self.touching(Dog):\n    self.say(\"Go to the dog!\")\n```",
    "stage": "Report whether `your_test` is *false*.  You often use this as part of an `if` or `while` statement.\n\n```\n# If the player isn't pressing \"a\",\n# give them an instruction:\nif not self.key_pressed(\"a\"):\n    self.say(\"Go to the dog!\")\n```"
   }
  },
  "scratch": "<not <touching (Dog v) ?>"
 },
 {
  "slug": "operators_string-concat",
  "kind": "block",
  "python": "`string_value_1` + `string_value_2`",
  "help": "Give the result of joining two strings together.\n\n```\n# Greet player:\nprint(\"Hello, \" + name)\n```\n\nIn Python, you can easily join more than two strings together:\n\n```\n# Announce winner:\nprint(\"Game over\")\nprint(\"and \" + name + \" won!\")\n```\n",
  "scratch": "(join [Hello, ] (name))"
 },
 {
  "slug": "operators_char-at",
  "kind": "block",
  "python": "`string_value`[`index`]",
  "help": "Give the letter (or number, or other character) at position `index` of the `string_value`.  Remember that Python starts counting at *zero*, so to get the first letter use `0` for `index`; to get the third letter, use `2` for `index`, and so on.\n\n```\n# Print the third letter of the\n# player's name:\nprint(name[2])\n```",
  "scratch": "(letter [3] of (name))"
 },
 {
  "slug": "operators_length",
  "kind": "block",
  "python": "len(`string_value`)",
  "help": "Give the number of letters (or numbers, or other characters) in the `string_value`.\n\n```\n# Tell the player how long their name is:\nprint(len(name))\n```",
  "scratch": "(length of (name))"
 },
 {
  "slug": "operators_contains",
  "kind": "block",
  "python": "`letters` in `string_value`",
  "help": "Report whether the `string_value` contains the string of `letters`.  You often use this as part of an `if` or `while` statement.\n\n```\n# If the player's favourite colour has\n# \"lu\" in it, say so:\nif \"lu\" in colour:\n    print(\"That has LU in\")\n```\n\nAn important difference is that Scratch ignores whether the letters are capitals or lower case, but in Python, \"A\" is different to \"a\".",
  "scratch": "<(colour) contains [lu] ?>"
 },
 {
  "slug": "operators_modulo",
  "kind": "block",
  "python": "`value_1` % `value_2`",
  "help": "Give the remainder after dividing `value_1` by `value_2`.\n\n```\n# Say how many loose eggs left over\n# after putting into boxes of 12:\nprint(n_eggs % 12)\n```",
  "scratch": "((n-eggs) mod [12])"
 },
 {
  "slug": "operators_round",
  "kind": "block",
  "python": "round(`value`)",
  "help": "Give the nearest whole number to `value`.\n\n```\n# Tell player the length of their\n# journey to the nearest whole km:\nprint(round(journey_length))\n```",
  "scratch": "(round (journey-length))"
 },
 {
  "slug": "operators_absolute",
  "kind": "non-method-block",
  "heading": "Mathematical functions",
  "help": {
   "flat": "Work out a mathematical function of the `value`.  In Python, you use these function:\n* `abs(x)` — Scratch's *abs of x*\n* `math.floor(x)` — *floor of x*\n* `math.ceil(x)` — *ceiling of x*\n* `math.sqrt(x)` — *sqrt of x*\n* `math.sin(x)` — *sin of x* — (✻)\n* `math.cos(x)` — *cos of x* — (✻)\n* `math.tan(x)` — *tan of x* — (✻)\n* `math.asin(x)` — *asin of x* — (✻)\n* `math.acos(x)` — *acos of x* — (✻)\n* `math.atan(x)` — *atan of x* — (✻)\n* `math.log(x)` — *ln of x*\n* `math.log10(x)` — *log of x*\n* `math.exp(x)` — *e ^ x*\n* `10 ** x` — *10 ^ x*\n\nTo use the functions starting with `math.`, you must have the line `import math` at the top of your program, after the `import pytch` line.\n\n(✻) An important difference is that Scratch's trigonometry functions work in *degrees* but Python's work in *radians*.",
   "per-method": "Work out a mathematical function of the `value`.  In Python, you use these function:\n* `abs(x)` — Scratch's *abs of x*\n* `math.floor(x)` — *floor of x*\n* `math.ceil(x)` — *ceiling of x*\n* `math.sqrt(x)` — *sqrt of x*\n* `math.sin(x)` — *sin of x* — (✻)\n* `math.cos(x)` — *cos of x* — (✻)\n* `math.tan(x)` — *tan of x* — (✻)\n* `math.asin(x)` — *asin of x* — (✻)\n* `math.acos(x)` — *acos of x* — (✻)\n* `math.atan(x)` — *atan of x* — (✻)\n* `math.log(x)` — *ln of x*\n* `math.log10(x)` — *log of x*\n* `math.exp(x)` — *e ^ x*\n* `10 ** x` — *10 ^ x*\n\n(✻) An important difference is that Scratch's trigonometry functions work in *degrees* but Python's work in *radians*."
  },
  "scratch": "([abs v] of (value))"
 },
 {
  "kind": "heading",
  "sectionSlug": "variables",
  "heading": "Working with variables"
 },
 {
  "slug": "variables_set-value",
  "kind": "block",
  "python": "`variable` = `value`",
  "help": "Make `variable` refer to `value`.\n\n```\n# Set the starting number of apples:\nn_apples = 5\n```",
  "scratch": "set [n-apples v] to [5]"
 },
 {
  "slug": "variables_increase-value",
  "kind": "block",
  "python": "`variable` += `value_increase`",
  "help": "Add `value_increase` to the value currently referred to by `variable`.\n\n```\n# The player got three more pears:\nn_pears += 3\n```\n\nIf you want to *decrease* a variable, you can use the `-=` operator.\n\n```\n# The player lost two bananas:\nn_bananas -= 2\n```\n\nYou can use `*=` and `/=` in a similar way.\n\nIn Python, the `+` operator can join strings or lists as well as add numbers, and the `+=` operator works the same.\n\n```\n# Build up greeting:\ngreeting = \"Hello, \"\ngreeting += player_name\n```",
  "scratch": "change [n-pears v] by [3]"
 },
 {
  "slug": "variables_show-variable",
  "kind": "block",
  "python": "self.show_variable(`variable_name`)",
  "help": {
   "flat": "Display a variable-watcher for the variable called `variable_name` belonging to the `self` sprite (or stage).  Usually, `variable_name` will be a literal string, being the name of the variable you would like to show.  A particular variable is only ever shown once, even if `show_variable()` is called many times for that variable.\n\n```\n# Show my health:\nself.show_variable(\"health\")\n```\n\nTo show a global variable, use this alternative form:\n\n```\n# Show the global \"score\" variable:\npytch.show_variable(None, \"score\")\n```\n\n(You might see this general `pytch.show_variable()` form elsewhere in older code.)",
   "per-method": {
    "sprite": "Display a variable-watcher for the variable called `variable_name` belonging to the `self` sprite.  Usually, `variable_name` will be a literal string, being the name of the variable you would like to show.  A particular variable is only ever shown once, even if `show_variable()` is called many times for that variable.\n\n```\n# Show my health:\nself.show_variable(\"health\")\n```\n\nTo show a global variable, use this alternative form:\n\n```\n# Show the global \"score\" variable:\npytch.show_variable(None, \"score\")\n```\n\n(You might see this general `pytch.show_variable()` form elsewhere in older code.)",
    "stage": "Display a variable-watcher for the variable called `variable_name` belonging to the stage.  Usually, `variable_name` will be a literal string, being the name of the variable you would like to show.  A particular variable is only ever shown once, even if `show_variable()` is called many times for that variable.\n\n```\n# Show the high-score, kept by the Stage:\nself.show_variable(\"hi_score\")\n```\n\nTo show a global variable, use this alternative form:\n\n```\n# Show the global \"score\" variable:\npytch.show_variable(None, \"score\")\n```\n\n(You might see this general `pytch.show_variable()` form elsewhere in older code.)"
   }
  },
  "scratch": "show variable [health v]"
 },
 {
  "slug": "variables_hide-variable",
  "kind": "block",
  "python": "self.hide_variable(`variable_name`)",
  "help": {
   "flat": "Hide the variable-watcher for the variable called `variable_name` belonging to the `self` sprite (or stage), if such a watcher exists.  If that variable is not being shown, do nothing.\n\nUsually, `variable_name` will be a literal string, being the name of the variable you would like to hide.\n\n```\n# Hide the high score:\nself.hide_variable(\"hi_score\")\n```\n\nTo hide a global variable, use this alternative form:\n\n```\n# Hide the global \"score\" variable:\npytch.hide_variable(None, \"score\")\n```\n\n(You might see this general `pytch.hide_variable()` form elsewhere in older code.)",
   "per-method": {
    "sprite": "Hide the variable-watcher for the variable called `variable_name` belonging to the `self` sprite, if such a watcher exists.  If that variable is not being shown, do nothing.\n\nUsually, `variable_name` will be a literal string, being the name of the variable you would like to hide.\n\n```\n# Hide my health:\nself.hide_variable(\"health\")\n```\n\nTo hide a global variable, use this alternative form with `None` for `owner`.\n\n```\n# Hide the global \"score\" variable:\npytch.hide_variable(None, \"score\")\n```\n\n(You might see this general `pytch.hide_variable()` form elsewhere in older code.)",
    "stage": "Hide the variable-watcher for the variable called `variable_name` belonging to the stage, if such a watcher exists.  If that variable is not being shown, do nothing.\n\nUsually, `variable_name` will be a literal string, being the name of the variable you would like to hide.\n\n```\n# Hide the high-score, kept by the Stage:\nself.hide_variable(\"hi_score\")\n```\n\nTo hide a global variable, use this alternative form with `None` for `owner`.\n\n```\n# Hide the global \"score\" variable:\npytch.hide_variable(None, \"score\")\n```\n\n(You might see this general `pytch.hide_variable()` form elsewhere in older code.)"
   }
  },
  "scratch": "hide variable [hi-score v]"
 },
 {
  "kind": "heading",
  "sectionSlug": "lists",
  "heading": "Working with lists"
 },
 {
  "slug": "lists_append",
  "kind": "block",
  "python": "`things`.append(`thing`)",
  "help": "Add the `thing` as a new entry at the end of the list `things`.\n\n```\n# Record another size:\nsizes.append(\"large\")\n```",
  "scratch": "add [large] to [sizes v]"
 },
 {
  "slug": "lists_delete",
  "kind": "block",
  "python": "del `things`[`index`]",
  "help": "Delete the entry at position `index` from the list `things`.  Remember that Python starts counting at *zero*, so to delete the *second* element of a list, `index` should be `1`, and so on.\n\n```\n# Delete the third colour:\ndel colours[2]\n```",
  "scratch": "delete [3] of [colours v]"
 },
 {
  "slug": "lists_delete-all",
  "kind": "block",
  "python": "del `things`[:]",
  "help": "Delete all entries of the list `things`.\n\n```\n# Forget all high scores:\ndel high_scores[:]\n```",
  "scratch": "delete all of [high-scores v]"
 },
 {
  "slug": "lists_insert",
  "kind": "block",
  "python": "`things`.insert(`index`, `thing`)",
  "help": "Insert a new entry `thing` into the list `things`, so that `thing` ends up at position `index`.  Remember that Python starts counting at *zero*, so to insert a new entry in the *second* place of the list, `index` should be `1`, and so on.\n\n```\n# Add \"basketball\" at very start of\n# list of sports:\nsports.insert(0, \"basketball\")\n```",
  "scratch": "insert [basketball] at [1] of [sports v]"
 },
 {
  "slug": "lists_replace",
  "kind": "block",
  "python": "`things`[`index`] = `new_thing`",
  "help": "Replace the entry at position `index` in the list `things` with `new_thing`.  Remember that Python starts counting at *zero*, so to replace the *second* element of a list, `index` should be `1`, and so on.\n\n```\n# Replace third flower with a rose:\nflowers[2] = \"rose\"\n```",
  "scratch": "replace item [3] of [flowers v] with [rose]"
 },
 {
  "slug": "lists_element-at-index",
  "kind": "block",
  "python": "`things`[`index`]",
  "help": "Give the element at position `index` in the list `things`.  Remember that Python starts counting at *zero*, so to get the *second* element of a list, `index` should be `1`, and so on.\n\n```\n# Announce the second animal:\nprint(animals[1])\n```",
  "scratch": "item [2] of [animals v]"
 },
 {
  "slug": "lists_index-of-element",
  "kind": "block",
  "python": "`things`.index(`thing`)",
  "help": "Report the smallest index where `thing` can be found in the list `things`.  Remember that Python starts counting at *zero*, so if `thing` is the *second* element of the list, the value `1` will be reported.\n\n```\n# Tell the player which fruit is the apple:\nprint(fruits.index(\"apple\"))\n```\n\nScratch reports `0` if `thing` is not in the list, but Python *raises an exception*.",
  "scratch": "(item # of [apple] in [fruits v])"
 },
 {
  "slug": "lists_length",
  "kind": "block",
  "python": "len(`things`)",
  "help": "Report the number of elements in the list `things`.  This will be zero if the list is empty.\n\n```\n# Say how many vegetables there are:\nprint(len(vegetables))\n```",
  "scratch": "(length of [vegetables v])"
 },
 {
  "slug": "lists_contains",
  "kind": "block",
  "python": "`thing` in `things`",
  "help": "Report (as `True` or `False`) whether the list `things` contains `thing`.\n\n```\n# Beep if \"dots\" is one of the patterns:\nif \"dots\" in patterns:\n    self.start_sound(\"beep\")\n```",
  "scratch": "<[patterns v] contains [dots]?>"
 },
 {
  "slug": "lists_choice",
  "kind": "block",
  "python": "random.choice(`things`)",
  "scratchIsLong": true,
  "help": {
   "flat": "To use this function, you must have the line `import random` at the top of your program, just after the `import pytch` line.\n\nGive a random element from the list `things`.  For `things`, you can give a literal list, or use a variable which refers to a list.\n\nLiteral list, using []:\n\n```\n# Pick a random cat:\ncat = random.choice([\"Lulu\", \"Daisy\", \"Olive\"])\n```\nUsing a variable:\n\n```\n# Create list of cats:\nall_cats = [\"Lulu\", \"Daisy\", \"Olive\"]\n# Pick one at random:\ncat = random.choice(all_cats)\n```",
   "per-method": "Give a random element from the list `things`.  For `things`, you can give a literal list, or use a variable which refers to a list.\n\nLiteral list, using []:\n\n```\n# Pick a random cat:\ncat = random.choice([\"Lulu\", \"Daisy\", \"Olive\"])\n```\nUsing a variable:\n\n```\n# Create list of cats:\nall_cats = [\"Lulu\", \"Daisy\", \"Olive\"]\n# Pick one at random:\ncat = random.choice(all_cats)\n```"
  },
  "scratch": "(item (pick random (1) to (length of [cats v])) of [cats v])"
 },
 {
  "kind": "heading",
  "sectionSlug": "instances",
  "heading": "Sprites, clones, and the Stage"
 },
 {
  "slug": "instances_original",
  "kind": "pure-python",
  "python": "`cls`.the_original()",
  "help": "Give the original instance of the Sprite class `cls`.\n\n```\n# Say which costume the original Dog\n# is wearing:\nthe_dog = Dog.the_original()\nprint(the_dog.costume_name)\n```\n\nAs a convenience, the following Sprite properties have special behaviour, different to how properties normally work in Python:\n\n* `sound_volume`\n* `x_position`\n* `y_position`\n* `direction`\n* `size`\n* `costume_number`\n* `costume_name`\n* `distance_to_mouse`\n* `touching_mouse`\n\nCode which reads these properties on a Sprite *class* gives the value of that property for that Sprite's *original instance*.\n\nThis can be useful where, for example, one sprite needs to know where another sprite is.  Instead of the fiddly\n\n```\nprint(SpaceShip.the_original().x_position)\n```\n\nyou can instead just write\n\n```\nprint(SpaceShip.x_position)\n```\n\nIf your code is reading a Sprite's *own* properties, you should use the simpler and standard\n\n```\nprint(self.x_position)\n```\n\nYou can *not* use this shortcut to *write* (\"assign\") to any of another Sprite's properties.  E.g., this code will not work:\n\n```\n# Will give error!\nSpaceShip.x_position += 10\n```"
 },
 {
  "slug": "instances_all-clones",
  "kind": "pure-python",
  "python": "`cls`.all_clones()",
  "help": "Give a list of all clones of the Sprite class `cls`.  If `cls` has no clones, the list will be empty.\n\n```\n# Say how many Cat clones there are:\nprint(len(Cat.all_clones()))\n```"
 },
 {
  "slug": "instances_all-instances",
  "kind": "pure-python",
  "python": "`cls`.all_instances()",
  "help": "Give a list of all instances of the Sprite class `cls`.  The first element of the list will be the original `cls`, followed by any clones.  If `cls` has no clones, the list will contain just one element, i.e., the original `cls`.\n\n```\n# Say how many Apples there are\n# altogether:\nprint(len(Apple.all_instances()))\n```"
 },
 {
  "slug": "instances_the-only",
  "kind": "pure-python",
  "python": {
   "flat": "`stage_cls`.the_only()",
   "per-method": "Stage.the_only()"
  },
  "help": {
   "flat": "Give the only instance of the Stage class `stage_cls`.\n\n```\n# Say which backdrop the stage\n# is showing:\nthe_sky = Sky.the_only()\nprint(the_sky.costume_name)\n```\n\nAs a convenience, the following Stage properties have special behaviour, different to how properties normally work in Python:\n\n* `sound_volume`\n* `backdrop_number`\n* `backdrop_name`\n\nCode which reads these properties on the Stage *class* gives the value of that property for the (unique) Stage instance.\n\nThis can be useful where, for example, a sprite needs to know what backdrop the stage is using.  Instead of the fiddly\n\n```\nprint(MyStageClass.the_only().backdrop_name)\n```\n\nyou can instead just write\n\n```\nprint(MyStageClass.backdrop_name)\n```\n\nIf your code is in your Stage, you should use the simpler and standard\n\n```\nprint(self.backdrop_name)\n```\n\nYou can *not* use this shortcut to *write* (\"assign\") to any of the Stage's properties.  E.g., this code will not work:\n\n```\n# Will give error!\nMyStageClass.sound_volume = 0.8\n```",
   "per-method": "Give the only instance of the `Stage` class.\n\n```\n# Say which backdrop the stage\n# is showing:\nthe_sky = Stage.the_only()\nprint(the_sky.costume_name)\n```\n\nAs a convenience, the following Stage properties have special behaviour, different to how properties normally work in Python:\n\n* `sound_volume`\n* `backdrop_number`\n* `backdrop_name`\n\nCode which reads these properties on the Stage *class* gives the value of that property for the (unique) Stage instance.\n\nThis can be useful where, for example, a sprite needs to know what backdrop the stage is using.  Instead of the fiddly\n\n```\nprint(Stage.the_only().backdrop_name)\n```\n\nyou can instead just write\n\n```\nprint(Stage.backdrop_name)\n```\n\nIf your code is in the Stage, you should use the simpler and standard\n\n```\nprint(self.backdrop_name)\n```\n\nYou can *not* use this shortcut to *write* (\"assign\") to any of the Stage's properties.  E.g., this code will not work:\n\n```\n# Will give error!\nStage.sound_volume = 0.8\n```"
  }
 }
]